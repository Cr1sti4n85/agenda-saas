import { Button } from "@/components/ui/button";
import React from "react";

const ResetPassword = () => {
  return (
    <div>
      <Button>Solicitar cambio de contraseña</Button>
    </div>
  );
};

export default ResetPassword;
